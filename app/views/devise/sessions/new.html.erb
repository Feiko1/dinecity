<div style="background-color: #f3f3f3;">
  <!-- <div class="container"> -->
    <!-- <div class="row"> -->
      <div class="padding-form sign-in-wrapper">
        <hr>
        <h3 style="text-align: center;">Log in</h3>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <span class="fa fa-at errspan"></span>
          <%= f.input :email, required: false, autofocus: true %>
          <span class="fa fa-lock errspan"></span>
          <%= f.input :password, required: false %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>
        <div class="form-actions">
         <br>
          <%= f.button :submit, "Log in", class:"button-base", style:"width: 100px;" %>
        </div>
        <% end %>
          <hr>
          <br>
        <!-- <h4 style="text-align: center">Or</h4> -->
        <%= render "devise/shared/links" %>
      </div>
    </div>
  <!-- </div>
</div> -->

<!-- <input type="text" placeholder="  &#xf007;" style="font-family:Arial, FontAwesome" />
-->
<!-- ^ another way of adding icon -->
